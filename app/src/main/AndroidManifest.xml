<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.asus.mybus">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".login_user">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".home_page" />
        <activity android:name=".Timetable" />
        <activity android:name=".timetableofkatraj" />
        <activity android:name=".admin_activity" />
        <activity android:name=".Conductor_activity" />
        <activity android:name=".seats" />
        <activity android:name=".KS1" />
        <activity android:name=".KN1" />
        <activity android:name=".KP1" />
        <activity android:name=".Booking" />
        <activity android:name=".next_booking_page" />
        <activity android:name=".NK1" />
        <activity android:name=".NS1" />
        <activity android:name=".NP1" />
        <activity android:name=".PN1" />
        <activity android:name=".PK1" />
        <activity android:name=".PS1" />
        <activity android:name=".ticket_booked_menu" />
        <activity android:name=".timetableofnigadi" />
        <activity android:name=".timetableofPunestation" />
        <activity android:name=".Login_activity" />
        <activity android:name=".ForgetAndChangePasswordActivity" />
        <activity android:name=".Register" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".aboutus"/>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <!--Enter google map key here-->

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
    </application>

</manifest>